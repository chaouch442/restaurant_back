export enum ReservationStatus {
  ACTIVE = 'active',
  FINISHED = 'finished',
  CANCELLED = 'cancelled',
  REPORTED = 'reported',
  CONFIRMED = 'confirmed',
  CONFIRMED_BY_CUSTOMER = 'confirmedByCustomer'
}
